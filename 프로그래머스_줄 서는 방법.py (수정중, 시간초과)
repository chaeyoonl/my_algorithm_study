import itertools


def solution(n, k):
    answer = []
    lists = []
    pLists = []

    for i in range(1, n + 1):
        lists.append(i)

    pLists = map(list, itertools.permutations(lists))

    return answer

